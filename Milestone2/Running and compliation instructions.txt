The program has been pre compiled. To run, all you need to do is cd to the /src/build directory and do "./Milestone2OS"

If for any reason this does not work, you must navigate to /src/build and perform "cmake .." "make" "./Milestone2OS" using CMake 